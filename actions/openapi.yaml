openapi: 3.1.0
info:
  title: Cerberus Runner (ChatGPT + Gemini)
  version: "0.4.2"
servers:
  - url: https://YOUR_APP_HOST
paths: {}
components:
  securitySchemes:
    ApiKeyAuth:
      type: apiKey
      in: header
      name: X-API-Key
